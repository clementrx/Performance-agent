"""Packaged reference PerformanceModels (seed fixtures, read-only).

The seed models under data/seed/ are few-shot examples and test fixtures — a
sprinter, a 10k runner, a powerlifter, a footballer. They are NOT a support
gate: any sport is modeled by research filling the generic schema. Every seed
value is provenance-labeled `prior` (team-chosen); corpus citations are added in
the Phase 10 evidence pass, never fabricated here.
"""
