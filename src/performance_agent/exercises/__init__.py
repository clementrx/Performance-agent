"""Packaged exercise ontology (seed library, read-only).

data/seed_exercises.yaml ships ~120 fully attributed ExerciseDefinitions across
the squat/hinge/push/pull/lunge/carry/core families plus jumps, plyometrics,
sprints, Olympic-lift derivatives, machines and run/ride/swim modalities. Athletes
add their own to exercises/library.yaml; the athlete library takes precedence over
a seed with the same id. Seed attribution provenance is `prior` unless a corpus id
supports it.
"""
